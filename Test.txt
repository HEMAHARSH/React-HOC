from java.util import *
import sys

connect('{{ user_wlc }}', '{{ password_wlc }}',
        '{{ protocol_wlc }}://{{ server_wlc }}:{{ port_wlc }}')

domainRuntime()

servers = domainRuntimeService.getServerRuntimes()

output_data = "{\n"

for srv in servers:

    name = srv.getName()

    # ---- Health exactly as GUI ----
    healthObj = srv.getHealthState()
    healthState = healthObj.toString()   # <-- IMPORTANT (no manual mapping)

    # ---- Status exactly as GUI ----
    status = srv.getState()

    print("Server:", name)
    print("Health:", healthState)
    print("Status:", status)
    print("--------------------------------")

    output_data += '  "' + name + '": {\n'
    output_data += '    "health": "' + healthState + '",\n'
    output_data += '    "status": "' + status + '"\n'
    output_data += '  },\n'

# Remove trailing comma
output_data = output_data.rstrip(',\n') + "\n}"

print('Current state of applications:\n', output_data)

output_file_path = r'D:\apps\{{ app_path }}\applications\AdminServer\server_healthcheck_output.json'

output_file = open(output_file_path, 'w')
output_file.write(output_data)
output_file.close()

print('Server health JSON generated at:', output_file_path)

exit()
